{% comment %}
  The contents of the collection.liquid template can be found in /sections/collection-template.liquid
{% endcomment %}
{%- section 'definitions-new2' -%}
{%- section 'my-region-collection-template' -%}

<script>
  // Override default values of shop.strings for each template.
  // Alternate product templates can change values of
  // 
  theme.productStrings = {
    sortBy: '{{ collection.sort_by | default: collection.default_sort_by }}'
  }
</script>

<style>
  {% if collection.handle contains 'my-products' %}
  	#nav-2 li:nth-of-type(1) button{background-color: {{ settings.color_primary }};}
  {% elsif collection.handle contains 'edited' %}
  	#nav-2 li:nth-of-type(4) button{background-color: {{ settings.color_primary }};}
  {% endif %}
</style>
